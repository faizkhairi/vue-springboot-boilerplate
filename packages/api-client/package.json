{
  "name": "@repo/api-client",
  "version": "1.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "generate": "openapi-generator-cli generate",
    "clean": "rm -rf src/generated",
    "build": "npm run clean && npm run generate"
  },
  "dependencies": {
    "axios": "^1.7.9"
  },
  "devDependencies": {
    "@openapitools/openapi-generator-cli": "^2.15.10",
    "typescript": "^5.9.2"
  }
}
